# step41全距离高斯谱宽地杂波改造

## Summary

- 保留 `step40` 作为“单距离散射体、LFM脉压和旁瓣”验证。
- 将 `step41` 改造成全距离分布式地杂波模型：脉压前生成、896个距离单元等平均功率、随机实现相互独立，慢时间采用零中心高斯功率谱。
- 继续比较 `σ_D=0.5/1.0/2.0 bin`，不使用AR(1)、白杂波基线、`R^-4`功率衰减或8192点补零。

## Implementation Changes

- 建立 `256×1022` 的扩展距离-慢时间散射矩阵，其中1022包含896个有效距离单元及左右各63个保护单元；矩阵保持项目约定的“脉冲×距离”排列。
- 每种谱宽由高斯功率谱对应的 `256×256` 慢时间协方差生成：
  \[
  R[\ell]=\exp[-2\pi^2(\sigma_D/256)^2\ell^2],\qquad C=LZ
  \]
  `Z`在距离维独立，三种谱宽共用同一白高斯矩阵以便公平比较。
- 每个脉冲依次经过64点归一化LFM卷积和匹配滤波；按压缩核能量归一化，使有效距离单元脉压后的期望功率仍为1。随后裁取中心896个距离单元，保护单元用于消除两端卷积功率亏损。
- 对脉压后 `256×896` 数据沿第一维加汉明窗并执行实际256点MTD。全距离平均谱与理论 `diag(F_wRF_w^H)` 对比；bin448仅作为诊断切片，不再代表唯一杂波位置。
- 输出切换到新的 `gaussian_clutter_full_range_width_sweep` 目录，保留现有单距离结果：
  - 3张全距离RD图、3张单案例谱图、1张三谱宽平均谱对比图、1张距离功率对比图。
  - 3个单案例256点谱CSV、合并768行谱表、2688行距离功率表、3行摘要表和输出索引。
  - MAT缓存保存配置、有效区脉压前杂波、脉压后数据、MTD结果和统计指标，不保存中间完整原始卷积矩阵。
- 更新 `AGENTS.md` 中 `step41` 的模型说明、输出位置和实测指标。

## Test Plan

- 使用MATLAB R2025b运行 `checkcode`，再用独立 `MATLAB_PREFDIR` 和 `MW_DDUX_FORCE_DISABLE=1` 完整执行脚本。
- 断言每种案例的脉压前有效区、脉压后数据和MTD结果均为 `256×896`，数值有限且每个距离单元能量非零。
- 验证压缩核归一化增益为1、保护单元裁剪长度正确，距离两端不存在确定性的卷积功率下降。
- 验证统计平均谱峰位于bin129附近，汉明窗后谱宽随 `0.5→1→2 bin` 单调增加，并输出理论FWHM与测得标准差。
- 核对8张PNG、7个CSV和1个MAT缓存均存在；谱表和距离表行数分别为256/768和2688，并抽查RD图全896距离均有杂波。

## Assumptions

- “全距离相同”表示平均功率和谱宽相同，而不是复制同一条复数杂波序列。
- 不加入额外距离空间相关；匹配滤波会自然产生相邻距离单元相关性。
- 载频和波长尚未确认，模型以多普勒bin和现有 `0.71 m/s/bin` 为准；约5.16 GHz仅是反推值，不作为硬件参数。
- 固定随机种子保证结果可复现；噪声、目标和非高斯纹理均不在本次模型范围内。
