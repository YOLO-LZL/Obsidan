---
title: 阿里-CTO线-实习-Java一面
source: https://ls8sck0zrg.feishu.cn/wiki/wikcnB75FvUa9BRGDC6Yzu4a5Mf
created: 2026-05-25
source_updated: 2024-04-10
tags:
  - clippings
  - 面经
  - 阿里
  - Java
---

# 阿里-CTO线-实习-Java一面

> [!info]
> 来源：[飞书云文档](https://ls8sck0zrg.feishu.cn/wiki/wikcnB75FvUa9BRGDC6Yzu4a5Mf)
> 最近修改：2024-04-10
> 整理时间：2026-05-25
> 说明：已去除飞书侧边栏与评论区噪音，按 Markdown 做轻度整理。

## 背景介绍

| 项目 | 内容 |
|------|------|
| 面试公司 | 阿里巴巴 |
| 部门 | CTO线-DTC&CXO |
| 岗位类型 | 实习招聘 |
| 第几面 | 一面 |
| 方向 | 后端开发 |
| 语言 | Java |
| 投递渠道 | 内推 |
| 面试人 | XX |
| 面试时间 | 2023-03-28 |

## 问题记录

1. 介绍一下你自己

2. 问对哪个项目熟悉。为什么想要用java去写关系型数据库？
    a. 这个数据库体现了Spring的事务传播机制吗？
    没有体现，只体现了mysql的两种事务隔离级别，一个可重复读，一个已提交读
    b. 这两个隔离级别表现的现象是什么，区别是什么样的？
    已提交读只能读取其他事务已经提交的数据，但是这个隔离级别就会造成一个不可重复读和幻读的现象，不可重复读就是消除了不可重复读和幻读的现象。
    c. 数据管理里，数据文件大体分成哪几种数据文件？
    因为是聚簇索引，所以只有一个文件，索引和数据是放在一起的。就是一个bd文件
    d. 日志文件是分成了哪几种？
    日志文件不同于mysql，mysql有三种日志文件，binlog，redolog和undolog，这个实现没有binlog，只是将redolog和undolog放在一个文件里，只有一种日志文件
    e. 为什么没有像mysql文件一样设置三种日志文件呢？
    （没回答好）就说是跟着教程来写的，因为mysql奔溃了要恢复数据，是依靠增加日志的种类，这其实也可以通过增加日志的种类来做。
    f. 说下MVCC机制的原理？
    MVCC就是多版本并发控制，实现了读写的并发控制，在mysql通过readview 隐藏字段和undolog实现了，比如在可重复读里面，比如开启了一个事务，就生成了一个readview，然后记录现在active的事务，判断查询的数据在这个事务可不可读。

3. 索引的类型有哈希索引，B+树索引，而hash索引的时间复杂度是o1，那为什么我们一般情况下不使用哈希索引，而使用b+树索引呢？
    因为hash索引只能做一个等值查询，像范围查询是做不到的。还有哈希可能会出现hash碰撞的问题。
    还能想到其他的原因吗？
    哈希表数据的存储是无序的，哈希索引还无法用于排序。

4. 对一个慢sql怎么去排查？
    如果是在项目中，可以通过SpringAOP去查询这个接口运行的时间，如果是一个sql，可以通过explain的指令去查这个sql的执行计划。
    还可以通过慢查询日志

5. 索引字段是不是建的越多越好
    不是，建的的越多会占用越多的空间

6. java这一块对框架都是熟悉的吧？
    熟悉SSM，SpringBoot还在学中。
    MyBatis运用了哪些常见的设计模式？
    运用了工厂模式，创建sqlsession的时候用了工厂模式，其他的没想起来
    MyBatis中创建了一个Mapper接口，在写一个xml文件，java的接口是要实现的，为什么这没有实现呢？
    （现在回想起来，是要诱导我说动态代理）一个mapper接口通过namespace的id对应的就是一个xml的文件。没了解背后的原理
    相比JDBC，MyBatis的优点？
    有些功能封装的更好，像打开一个sqlsession的连接，而且写的代码要少
    JDBC连接数据库的步骤
    不记得了，太久没用

7. 怎么理解SpringIoc？
    SpringIoc就是控制反转，把创建对象的权力交给了框架，然后Spring通过反射就会创建一个对象。当我创建的一个类要用到这个对象，Spring就会把这个对象交给我
    如果让你设计一个SpringIoc，你觉得会从哪些方面考虑这个设计？
    多线程，这个对象是单例的还是每个线程都特有的。
    可能会有循环依赖的问题，像对象A有B的引用，然后对象B有A的引用
    bean的生命周期也需要考虑。其他的想不到了
    Spring给我们提供了很多扩展点，这些有了解吗？
    不太清楚扩展点指的什么
    就比如beanfactory，init方法这些（没太听清）

8. servlet有写过简单的代码
    没用框架之前，就用的servlet，用了框架就用的框架
    大致了解SpringMVC的处理流程吗？
    首先通过一个dispatchservlet去转接请求，到handlermapping去返回一个执行链，就比如拦截器到哪个controller，返回以后就到handler，获取这个请求要求的controller，然后去controller这里返回一个数据或者页面modelandview，然后给前端。
    Handlermapping 和 handleradapter有了解吗
    只知道它们的职责，编程中没有太多接触

9. SpringAOP主要想解决什么问题
    提供了一个扩展功能，可以一个类的某个方法进行加强，比如在之前加强，在之后加强，环绕加强。
    SpringAOP的原理了解吗
    基于一个动态代理的设计模式，如果动态加强的类实现了某个接口，就会用JDK动态代理，如果是对于没有实现接口的类，就会用CGLIB动态代理模板，去生成一个被代理对象的一个子类来作为代理对象
    动态代理和静态代理的区别
    静态代理是自己手写一个代理类，但是动态代理不需要直接实现这个代理类的，相当于静态代理在编译的时候就已经变成了一个个二进制文件了，动态代理在运行的时候动态生成类的字节码文件。
    动态代理中如果有实现了接口，就会用JDK动态代理
    不太了解（后来查了应该是因为JDK动态代理会继承Proxy类，但是java是单继承）

10. 代理模式主要是去加强一个类的方法。
    代理模式和适配器模式有什么区别
    是接口转换成一个想要的接口（这个问题被面试说回答的不好）
    总的来说，代理模式和适配器模式虽然有些相似，但是它们的本质不同，应用场景也不同。代理模式是为了增强目标对象的功能或者保护目标对象，而适配器模式是为了让原本不兼容的接口能够协同工作。

11. java线程的生命周期有了解吗？
    new就是创建一个线程，变成ready的状态，如果分配到时间片，就会是一个运行的状态，等待其他线程做出一个动作就是waiting的状态，如果是等待其他资源的释放，就是block的状态，最后是一个终止的状态。
    使用多线程要注意哪些问题？
    保证数据的可见性或者多个线程对这个数据的一致性
    保证数据的一致性有哪些方案呢？
    比如有violate修饰一个变量，或者sychonized或者加锁
    线程池有了解吗？线程池大概的原理？
    分为核心线程池，线程池的最大容量，还有等待任务的队列，提交一个任务，如果核心线程没有满，就创建一个线程，如果满了，就是会加入等待队列，如果等待队列满了，就会增加线程，如果达到最大线程数量，如果都达到最大线程数量，就会按照一些丢弃的策略进行处理

12. ArrayList和LinkedList有什么区别
    通过数组存储数据，如果查询的话是会快速定位到这个数据，但是新增和删除数据会涉及数据的移动，像linkedList就是双向链表，定位数据会一个个查，新增和删除是一个o1
    把ArrayList变成线程安全有哪些方法
    线程不安全，使用它的Vector
    （被问还有呢？）
    将ArrayList变成线程安全有几种方法：
    1. 使用Collections.synchronizedList()方法将ArrayList转换为线程安全的List。该方法会返回一个线程安全的List，使用该List时需要在访问它的方法上添加synchronized关键字，以保证多线程访问的安全性。
    2. 使用CopyOnWriteArrayList类来代替ArrayList。CopyOnWriteArrayList是一种线程安全的List实现，它通过在写操作时复制整个数组来保证线程安全性，在读操作时不需要加锁，因此可以提高读取效率。
    3. 使用Lock接口来实现同步。可以使用ReentrantLock类来实现对ArrayList的同步操作，该类提供了与synchronized类似的功能，但是具有更高的灵活性。比如可以使用tryLock()方法来尝试获取锁，避免了线程的长时间等待。
    4. 使用读写锁来实现同步。可以使用ReentrantReadWriteLock类来实现对ArrayList的读写操作的同步。该类提供了读锁和写锁两种锁，多个线程可以同时获取读锁，但是只有一个线程可以获取写锁，在写操作时需要先获取写锁，以保证线程安全。

13. http协议的报文的格式有了解吗？
    不是太清楚，有报文头，报文体，如果是post请求就会在报文体写上数据
    请求行、请求头、请求体。
    http常用的状态码？
    4XX是请求报文有误，5XX是服务器有错，2XX是成功的。3XX忘了（是重定向）

14. 对面向对象的理解？
    像面向过程就是把问题分解成一个一个函数，然后调用函数去解决问题。而面向对象就是把这个世界抽象成一个一个对象，然后赋予这些对象一个属性，成员变量和方法，然后去调用对象的方法去解决问题，
    面向过程的方法存在哪些问题？
    比较高，分工不好分工。（不知道怎么回答好）
    但像操作系统也是用c语言写的，它就是面向过程的
    面向过程也有他的好处把
    好处是什么？
    解决问题思路比较简单。

15. 读书中遇到最难的技术是什么，怎么克服的？
    说了自己科研的方面
    有没有什么强项在面试中还没有展现的？
    没了（这个应该怎么回答呀？）
    可以讲一些软实力，擅长沟通、协调资源、快速学习的能力这些，需要举例说明

## 感觉

面试官有引导，大多问的是八股，会的就回答的比较流畅，不熟悉的就磕磕巴巴

## 面试总结

面试官给的反馈基础还行，但是深度不够，对刚刚设计模式的对比回答不满意，和mybatis的原理回答也不太满意

## 不足之处

对框架还是不够熟练

回答不够全面，经常被面试官问还有呢，但就回答不上来了

网络这方面看了就忘

面试官建议讲项目从以下四点：项目的背景、核心解决思路、技术上的挑战点、最后取得什么结果。
