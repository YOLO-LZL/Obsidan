## 规范

- 优先通过 obsidian-cli skill 调用命令
- 所有命令默认加上-标签搜索优先使用silent（除非特别说明要在前台打开）
- 标签搜索优先使用obsidian tag命令，比全文搜索更精确
## 网页内容抓取规则（虚拟滚动/懒加载页面）
- 整理格式按照@字节-商业平台-实习-Java三面
- **禁止**一次性 `innerText` 后直接保存，必须边滚动边累积去重
- 累积策略：每步提取可见文本块，用「前 40 字符做 hash」去重存入 Map
- 停止条件（满足任一即停）：
  1. `scrollTop + clientHeight >= scrollHeight - 10`（已到底部）
  2. 连续 4 次滚动无新增文本
  3. 末尾出现「总结/参考资料/不足之处/评论/完/点赞」等尾部标记
- 写入 MD 前按文本段去重：飞书等页面会在 DOM 中重复渲染同一内容
- 严格保留原文，不做任何改写